# =============================================================================
# Claude Code Web Environment
# =============================================================================
# Only 2 tokens needed in CC Web - minimal attack surface

# GitHub token - ACTIONS ONLY (for workflow dispatch)
# Create at: github.com/settings/personal-access-tokens/new
# Repository: Select this repo only
# Permissions: Actions: Read and write (ONLY this - nothing else!)
# GH_TOKEN=ghp_...

# Browserless token - Use a FREE account only
# Create at: browserless.io (free tier)
# Warning: Token cannot be scoped, so use a dedicated free account
# BROWSERLESS_TOKEN=...

# =============================================================================
# GitHub Repository Secrets (NOT here - add via Settings → Secrets → Actions)
# =============================================================================
# These are used by GitHub Actions workflows, not CC Web:
#
# VERCEL_TOKEN      - vercel.com/account/tokens (team-scoped)
# VERCEL_ORG_ID     - Found in Vercel dashboard
# VERCEL_PROJECT_ID_<NAME> - Per-project IDs from vercel-setup workflow
